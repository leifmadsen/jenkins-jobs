- defaults:
    name: ci-workflow-provision-rdo-overcloud
    description: |
        Managed by Jenkins Job Builder. Do not edit via web.
    concurrent: true
    scm:
      - clone-oooq
      - clone-collect-logs
    wrappers:
        - default-ci-workflow-wrappers-rdo-overcloud

- wrapper:
    name: default-ci-workflow-wrappers-rdo-overcloud
    wrappers:
      - copy-to-slave:
          includes:
            - '**/config/**'
          relative-to: 'userContent'
      - build-timeout:
          timeout: 180
      - timestamps
      - workspace-cleanup

- publisher:
    name: default-ci-workflow-publishers-rdo-overcloud
    publishers:
        - publish-to-static:
              publish_target: 'rdo_overcloud_{release}_deploy_{partner}_logs/${{BUILD_ID}}'
