- job-template:
    name: 'oooq-{release}-deploy-{partner}'
    node: '{partner}'

    scm:
      - clone-oooq
      - clone-collect-logs
    builders:
      - cleanup-environment
      - oooq-clear-colors
      - oooq-install-deps
      - oooq-export-vars
      - oooq-deploy:
          working-dir: "/home/stack/quickstart"
          requirements: "${{WORKSPACE}}/config/requirements/toad-requirements.txt"
          config: "./config/general_config/minimal.yml"
          release: "{release}"
          playbook: "quickstart-extras.yml"
          extras: ""
      - collect-ara-results
      - collect-logs
      - collect-exit-code
    wrappers:
      - copy-to-slave:
          includes:
            - '**/config/**'
          relative-to: 'userContent'
      - build-timeout:
          timeout: 180
      - timestamps
      - workspace-cleanup

    publishers:
      - publish-to-static:
          publish_target: 'oooq_{release}_deploy_{partner}_logs/${{BUILD_ID}}'

- job-template:
    name: 'oooq-validate-{partner}'
    node: '{partner}'

    scm:
      - clone-oooq
      - clone-collect-logs
    builders:
      - oooq-clear-colors
      - oooq-export-vars
      - shell: |
          socketdir=$(mktemp -d /tmp/sockXXXXXX)
          # copy ansible inventory from previous deploy
          cp /home/stack/quickstart/hosts ${{WORKSPACE}}/
      - inject:
          properties-content: |
            ANSIBLE_SSH_CONTROL_PATH=$socketdir/%%h-%%r
            SSH_CONFIG=/home/stack/quickstart/ssh.config.ansible
            ANSIBLE_SSH_ARGS="-F ${{SSH_CONFIG}}"
      - oooq-validate:
          working-dir: "$WORKSPACE"
          requirements: "$WORKSPACE/config/requirements/toad-requirements.txt"
          playbook: "tempest.yml"
          extras: "--extra-vars tempest_source=rdo --extra-vars tempest_format=venv --extra-vars run_tempest=true"
      - collect-ara-results
      - collect-logs
      - collect-exit-code
    wrappers:
      - copy-to-slave:
          includes:
            - '**/config/**'
          relative-to: 'userContent'
      - build-timeout:
          timeout: 180
      - timestamps

    publishers:
      - publish-to-static:
          publish_target: 'oooq_validate_{partner}_logs/${{BUILD_ID}}'

    triggers:
      - reverse:
          jobs:
            - 'oooq-liberty-deploy-{partner}'
            - 'oooq-mitaka-deploy-{partner}'
            - 'oooq-newton-deploy-{partner}'
            - 'oooq-master-deploy-{partner}'
        result: 'success'

- job-template:
    name: 'oooq-{release}-deploy-baremetal-{hwenv}'
    node: '{hwenv}'

    scm:
      - clone-oooq
      - clone-collect-logs
    builders:
      - cleanup-environment
      - oooq-clear-colors
      - oooq-install-deps
      - oooq-export-vars
      - oooq-deploy:
          working-dir: "/home/stack/quickstart"
          requirements: "${{WORKSPACE}}/config/requirements/toad-requirements.txt"
          config: "./config/general_config/minimal.yml"
          release: "{release}"
          playbook: "baremetal-full-deploy.yml"
          extras: "--config ${{WORKSPACE}}/toad_envs/{hwenv}/deploy_config.yml --extra-vars @${{WORKSPACE}}/toad_envs/{hwenv}/env_settings.yml"
      - collect-ara-results
      - collect-logs
      - collect-exit-code
    wrappers:
      - copy-to-slave:
          includes:
            - '**/config/**,**/toad_envs/**'
          relative-to: 'userContent'
      - build-timeout:
          timeout: 180
      - timestamps
      - workspace-cleanup

    publishers:
      - publish-to-static:
          publish_target: 'oooq_{release}_deploy_baremetal_{hwenv}_logs/${{BUILD_ID}}'

- job-template:
    name: 'oooq-{release}-upgrade-next-baremetal-{hwenv}'
    node: '{hwenv}'

    scm:
      - clone-oooq
      - clone-collect-logs
    builders:
      - cleanup-environment
      - oooq-clear-colors
      - oooq-install-deps
      - oooq-export-vars
      - oooq-deploy:
          working-dir: "/home/stack/quickstart"
          requirements: "${{WORKSPACE}}/config/requirements/toad-requirements.txt"
          config: "./config/general_config/minimal.yml"
          release: "{release}"
          playbook: "upgrade-baremetal.yml"
          extras: "--config ${{WORKSPACE}}/toad_envs/{hwenv}/deploy_config.yml \
                   --extra-vars @${{WORKSPACE}}/toad_envs/{hwenv}/env_settings.yml \
                   --extra-vars upgrade_delorean_hash=current-passed-ci \
                   --extra-vars deployment_type=baremetal \
                   --extra-vars major_upgrade=true \
                   --extra-vars enable_pacemaker=false \
                   --extra-vars target_upgrade_version=ocata \
                   --extra-vars set_overcloud_workers=false"
      - collect-ara-results
      - collect-logs
      - collect-exit-code
    wrappers:
      - copy-to-slave:
          includes:
            - '**/config/**,**/toad_envs/**'
          relative-to: 'userContent'
      - build-timeout:
          timeout: 180
      - timestamps
      - workspace-cleanup

    publishers:
      - publish-to-static:
          publish_target: 'oooq_{release}_upgrade_next_baremetal_{hwenv}_logs/${{BUILD_ID}}'
